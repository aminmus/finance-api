# Migration `20200901140241-change-field-names`

This migration has been generated by Amin Mushehid at 9/1/2020, 4:02:41 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200830161713-rename-asset-types..20200901140241-change-field-names
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -17,39 +17,39 @@
   portfolios Portfolio[]
 }
 model Asset {
-  id                   Int                  @id @default(autoincrement())
-  updatedAt            DateTime             @updatedAt @default(now())
-  createdAt            DateTime             @default(now())
-  name                 String
-  description          String?
-  quantity             Int                  @default(0)
-  portfolio            Portfolio            @relation(fields: [portfolioId], references: [id])
-  portfolioId          Int
-  transaction          Transaction[]
-  PrivateAsset PrivateAsset
-  PublicAsset  PublicAsset
+  id           Int           @id @default(autoincrement())
+  updatedAt    DateTime      @updatedAt @default(now())
+  createdAt    DateTime      @default(now())
+  name         String
+  description  String?
+  quantity     Int           @default(0)
+  portfolio    Portfolio     @relation(fields: [portfolioId], references: [id])
+  portfolioId  Int
+  transaction  Transaction[]
+  privateAsset PrivateAsset
+  publicAsset  PublicAsset
 }
 model PublicAsset {
-  id     Int     @unique
-  asset  Asset   @relation(fields: [id], references: [id])
-  symbol String?
-  market String?
+  id        Int     @unique
+  baseAsset Asset   @relation(fields: [id], references: [id])
+  symbol    String?
+  market    String?
 }
 model PrivateAsset {
   id               Int               @unique
-  asset            Asset             @relation(fields: [id], references: [id])
+  baseAsset        Asset             @relation(fields: [id], references: [id])
   historicalValues HistoricalValue[]
 }
 model HistoricalValue {
-  id        Int                  @id @default(autoincrement())
-  updatedAt DateTime             @updatedAt @default(now())
-  createdAt DateTime             @default(now())
-  date      DateTime             @default(now())
+  id        Int          @id @default(autoincrement())
+  updatedAt DateTime     @updatedAt @default(now())
+  createdAt DateTime     @default(now())
+  date      DateTime     @default(now())
   unitPrice Int
   asset     PrivateAsset @relation(fields: [assetId], references: [id])
   assetId   Int
 }
@@ -60,9 +60,9 @@
   createdAt   DateTime @default(now())
   name        String
   description String?
   assets      Asset[]
-  User        User     @relation(fields: [userId], references: [id])
+  owner       User     @relation(fields: [userId], references: [id])
   userId      Int
 }
 model Transaction {
@@ -72,9 +72,8 @@
   date            DateTime        @default(now())
   note            String?
   currency        String
   unitPrice       Int
-  //  totalPrice      Int
   assetQuantity   Int
   asset           Asset           @relation(fields: [assetId], references: [id])
   assetId         Int
   transactionType TransactionType
```


