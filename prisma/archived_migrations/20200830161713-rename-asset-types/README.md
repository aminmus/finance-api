# Migration `20200830161713-rename-asset-types`

This migration has been generated by Amin Mushehid at 8/30/2020, 6:17:13 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."PrivatelyTradedAsset" DROP CONSTRAINT "PrivatelyTradedAsset_id_fkey"

ALTER TABLE "public"."PubliclyTradedAsset" DROP CONSTRAINT "PubliclyTradedAsset_id_fkey"

ALTER TABLE "public"."HistoricalValue" DROP CONSTRAINT "HistoricalValue_assetId_fkey"

CREATE TABLE "public"."PublicAsset" (
"id" integer  NOT NULL ,
"symbol" text   ,
"market" text   )

CREATE TABLE "public"."PrivateAsset" (
"id" integer  NOT NULL )

CREATE UNIQUE INDEX "PublicAsset.id_unique" ON "public"."PublicAsset"("id")

CREATE UNIQUE INDEX "PrivateAsset.id_unique" ON "public"."PrivateAsset"("id")

ALTER TABLE "public"."PublicAsset" ADD FOREIGN KEY ("id")REFERENCES "public"."Asset"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."PrivateAsset" ADD FOREIGN KEY ("id")REFERENCES "public"."Asset"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."HistoricalValue" ADD FOREIGN KEY ("assetId")REFERENCES "public"."PrivateAsset"("id") ON DELETE CASCADE ON UPDATE CASCADE

DROP TABLE "public"."PrivatelyTradedAsset";

DROP TABLE "public"."PubliclyTradedAsset";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200829222359-privately-and-publicly-traded-assets..20200830161713-rename-asset-types
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -26,20 +26,20 @@
   quantity             Int                  @default(0)
   portfolio            Portfolio            @relation(fields: [portfolioId], references: [id])
   portfolioId          Int
   transaction          Transaction[]
-  privatelyTradedAsset PrivatelyTradedAsset
-  publiclyTradedAsset  PubliclyTradedAsset
+  PrivateAsset PrivateAsset
+  PublicAsset  PublicAsset
 }
-model PubliclyTradedAsset {
+model PublicAsset {
   id     Int     @unique
   asset  Asset   @relation(fields: [id], references: [id])
   symbol String?
   market String?
 }
-model PrivatelyTradedAsset {
+model PrivateAsset {
   id               Int               @unique
   asset            Asset             @relation(fields: [id], references: [id])
   historicalValues HistoricalValue[]
 }
@@ -49,9 +49,9 @@
   updatedAt DateTime             @updatedAt @default(now())
   createdAt DateTime             @default(now())
   date      DateTime             @default(now())
   unitPrice Int
-  asset     PrivatelyTradedAsset @relation(fields: [assetId], references: [id])
+  asset     PrivateAsset @relation(fields: [assetId], references: [id])
   assetId   Int
 }
 model Portfolio {
```


